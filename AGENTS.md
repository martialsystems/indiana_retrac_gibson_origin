# Agent notes: indiana_retrac_gibson_origin

Private. All rights reserved. Question: On Gibson origin-facility-quarter cells, do last year’s same-quarter shipments beat mileage-plus-population on held-out 2024?

Parent science lock `5800fc3` is a statewide citation only. Do not restamp it. Do not touch that git. Do not unpark `indiana_hazmat_floodplain`. `indiana_research_console` stays empty. Winter Pages stay off.

This tree is one origin: Gibson. Do not add Vermillion, Sullivan, Floyd, Randolph, or Newton. Do not start the inbound-host sequel here. Do not average cell assignment with the quarterly total. Do not promote a statewide win.

The extract is all reported Gibson-origin tons, including Restricted Waste Site Type I at permit 26-06. Do not email this PDF to `gcsw@gibsoncounty-in.gov`. Do not call the 98% on-site door a district finding. A filtered MSW/C&D memo (drop RWS Type I) is a new question, new bar, new n, not this lock. Parent `5800fc3` stays unfiltered.

The packet is the PDF, one CSV of the same 48 rows, an operator memo, and a five-line hold note. Do not send. README may cite `5800fc3` and state the question plus the two-answer rule. README may not ship county rows, facility IDs, or destination names. Write the packet from the frozen JSON (`scripts/write_packet.py`). Do not rescore.

Contestant is last year, same quarter. Bar is origin population over great-circle miles, J restricted to Gibson train-era destinations. Origin population cancels. Confirmation 2025 is out of train and out of J. The science lock is the public IDEM quarterly XLSX, not `app.re-trac.com`. Empty XLSX, unmatched origin county, missing facility coordinates, or missing CRS stop (`run_live.py` exit 2). Stage 0 fixture before live. Two figures max.

Sheet lock `c89de5b`. Confirmation 2025 stays out. Do not start Vermillion inbound in this repo. Do not re-run live onto `logs/in_live`.

`gibsonforge/` is the GraphForge tripwire. It refuses: Stage 0 missing before live; two answers averaged; README county rows; a Gibson cell table outside the buyer PDF; restamp of `5800fc3` or `c89de5b`; claim bans (casualties, climate attribution, population-at-risk, logistics optimized, truck routing, next-year Gibson total). Fixture RMSE, figure styling, and gist presence stay on pytest and vbd_gate.

## Verify

`python3 ~/agent_laws_verify_before_done/vbd_gate.py check --app-root . --claim-done`
